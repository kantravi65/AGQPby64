# App Update Service Config
# Set the GitHub repository path for updates (e.g., username/repo)
APP_UPDATE_REPO=kantravi65/AGQPby64

# Set the GitHub personal access token (with repo read permissions) for downloading updates
APP_UPDATE_TOKEN=

# GitHub SHA for version comparison
APP_GITHUB_SHA=
